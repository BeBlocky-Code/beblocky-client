/**
 * @deprecated Use `useCourses` / `useCourse` from `@/lib/hooks` (TanStack Query).
 * Kept as a thin re-export so any stray imports keep the shared cache.
 */
export { useCourses, useCourse } from "@/lib/hooks/use-courses";
